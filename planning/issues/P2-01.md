# P2-01 공통 화면·Tech 탐색·로그인

## 문제와 시작 상태

2026-09-26 확인: main·origin/main `e147f10`, 로컬 소스 변경 없음. P1-05A CI 36221577967·Pages 36221578032 성공. 기존 별도 PORT 계획 보존. 화면은 준비 안내와 아키텍처만 있고 분류·태그·권한별 읽기 API를 사용할 수 없는 상태.

## 구현 범위

- 핸드오프의 드릴 로고·헤더·푸터, Noto Sans KR/IBM Plex Mono, 카드·분류 트리·태그 탐색, 라이트/다크 테마와 반응형 공통 틀.
- Home/Tech의 실제 Tech 글 목록, 10개씩 추가 로드, 분류·태그 URL 필터와 뒤로가기, 로딩·빈 결과·API 미설정·실패 상태. 아직 없는 요약·집계·프로필·잔디·핀·Notes 혼합 데이터를 만들지 않음.
- 정적 `/tech/`, `/post/?slug=...`, `/login/` 경로. Projects/Notes는 실제 기능을 후속 구현할 준비 화면. `trailingSlash`와 Pages basePath, 직접 새로고침 지원.
- 글 상세의 제목·날짜·분류·태그·공개 범위와 기본 Markdown/GFM 읽기. raw HTML 실행 없음. 이미지 전달 API가 없는 동안 외부 이미지 자동 요청 없음. 코드·표 내부 스크롤, 읽기 전용 할 일.
- 기존 Spring Session JDBC의 현재 사용자·CSRF·로그인·로그아웃 연결. 계정명 입력 사용, JWT/토큰 영구 저장 없음. 익명 공개 요청은 쿠키 제외, 인증된 읽기는 credentials 포함. 로그아웃·401에서 민감한 화면 데이터 제거 및 진행 요청 폐기.
- 홈의 임시 아키텍처 도면 접근 유지. 기술 안내는 접을 수 있는 별도 영역에 배치, 실제 배포 경계 표기.
- README·실행 안내·전달 체크리스트 갱신. 학습 문서 추가/갱신 없음.

## 제외 범위

API 변경, 관리자 편집·분류 관리 화면, 검색·요약·핀·시리즈·조회수·프로필·활동 달력, Projects/Notes 데이터, 이미지 공개 전달, 고급 Markdown/블록 에디터, GA 연동은 후속 계획. 기존 VM 앱·DB·Redis·외부 설정 변경 없음. 공개 HTTPS API 주소가 없어 실사이트 로그인 연동 완료 판정 없음.

## 완료 조건

- 정적 출력에서 빌드 시 API 호출 없이 모든 경로 생성, basePath 배포와 직접 URL 정상.
- 샘플 운영 데이터 없이 실제 격리 API의 공개 목록·필터·글 상세와 회원 로그인 전후 차이 확인.
- CSRF 재발급·로그아웃·실패·세션 만료·늦은 응답 처리 확인. 쿠키/자격 증명 콘솔 노출 없음.
- 320~1440px의 기본 화면, 다크 모드·키보드 이동·포커스·페이지 가로 넘침 확인.
- 신규 테스트 코드 없이 기존 프론트 검사·타입 검사·정적 빌드 및 실제 브라우저 검증 통과.
- 커밋별 목적 구분, main 정상 push, 해당 SHA의 CI·Pages 결과 확인. author date와 실제 검증/반영 날짜 분리.

## 검증 방법

1. 기존 npm 검사와 타입 검사, Pages basePath 정적 출력.
2. 독립 MySQL과 최신 API JAR에 검증 전용 글·분류·계정 준비, 독립 정적 서버에서 브라우저 요청 확인.
3. 익명·회원·관리자 읽기, 잘못된 로그인·CSRF·로그아웃·401·미설정·통신 장애와 복구, raw HTML/위험 링크 입력 확인.
4. 모바일/데스크톱·다크/라이트·새로고침·뒤로가기·스크린샷 검토. 검증 자원만 정리, 기존 배포 identity/StartedAt 불변 확인.

## 참고

- 제공된 HANDOFF §3·6·9·10 및 Tech/로그인/본문의 데스크톱·모바일 스냅샷.
- [Next.js 정적 출력](https://nextjs.org/docs/app/guides/static-exports), [react-markdown](https://github.com/remarkjs/react-markdown).

## 실행 결과

2026-09-26 UTC 구현·격리 검증 완료, main 반영 전.

- 기존 npm 검사 7개, TypeScript 검사와 Next 정적 빌드 통과. API 설정·미설정 두 빌드에서 `/`, `/tech/`, `/post/`, `/login/`, `/projects/`, `/notes/` 정적 생성. 신규 테스트 코드 없음.
- 검증 전용 MySQL·기존 최신 API JAR·정적 서버와 Chrome 사용. 실제 공개 14개/비공개 1개 글로 10→14 자동 추가 로드와 중복 없는 목록, 분류·태그 URL 필터·새로고침·뒤로가기·빈 결과 확인. 운영 DB/배포 교체 없음.
- 기본 Markdown 표·코드·읽기 전용 할 일 확인. raw script 실행·위험 링크·외부 이미지 요청 차단. 익명 비공개 잠금에서는 본문·분류·태그 제외.
- 잘못된 비밀번호 거부, USER·ADMIN 로그인, CSRF 회전 후 원래 글로 복귀, 로그아웃 본문 제거, HttpOnly 쿠키·영구 토큰 저장 없음 확인. DB의 마지막 접근/만료 시각을 앞당겨 실제 JDBC 세션 만료 후 focus 재검사·잠금 확인. 자연 30분 경과 시험은 아님.
- 실제 서버가 반환한 회원 본문을 브라우저에서 지연시킨 뒤 로그아웃, DOM 변경 관찰에서 이전 본문 재등장 없음. API 중단 시 연결 오류·복구 후 재시도, 테마 재로드 유지, 키보드 skip link, 외부 returnTo 차단 확인.
- 별도 실제 HTTP 서버의 헤더 이후 본문 지연에서 8초 timeout, 호출자 AbortSignal 취소 확인. 초기 구현의 본문 읽기 제한 누락을 수정한 뒤 검증.
- Tech·본문·로그인의 320/360/390/768/1024/1280/1440px × 라이트/다크 페이지 넘침 0. Home·Tech·본문·로그인의 두 테마 axe 4.12.1 검사 8건 위반 0. 비문자 테마 아이콘 대비는 육안 검토. 최초 검사에서 발견한 제목 계층·버튼 대비·코드 스크롤 포커스 수정 후 재검사. 320px 긴 코드·8열 표의 ArrowRight 내부 스크롤 실제 확인.
- API 미설정 산출물은 구성 안내 표시, 실제 빈 목록처럼 0개를 만들지 않으며 API 요청 없음. 홈의 접이식 아키텍처 도면 두 테마 확인. 브라우저 uncaught error 없음.
- 최종 산출물은 공개 API 주소 미설정 상태. 공개 HTTPS API·실사이트 로그인 연결, 관리자 화면·고급 Markdown 등 제외 범위 미완료. 실제 VM 앱·Redis 유지.

검증 자료는 저장소 밖의 전용 작업 폴더에 보관. author date와 실제 검증 날짜 구분, 실행 전 상태를 과거 완료로 기록하지 않음.

## 커밋과 운영 경계

- `260172d feat(web): add session-aware API client` — author 2026-06-16 10:30 +09:00.
- `7cfc48b feat(web): build static blog browsing and login screens` — author 2026-06-16 17:40 +09:00.
- 문서 커밋 author는 2026-06-17, 실제 구현·검증·commit은 2026-09-26 UTC. push/CI/Pages는 원격 확인 후 별도 기록.
- 검증 전용 JAR·MySQL 컨테이너/볼륨·브라우저·HTTP 서버 종료. 기존 VM 앱·Redis의 ID·이미지·StartedAt 불변 확인. 복구용 로컬 bundle과 변경 파일 스냅샷 확보.

### 원격 1차 확인

2026-09-26 UTC `56cd47a` 정상 push, Pages 36223261422 성공과 6경로 HTTP 200 확인. CI 36223261409의 web 빌드/기존 검사 성공 뒤 HTTP smoke가 이전 `<title>Ken Blog</title>`를 기대해 실패. 실제 새 제목 `ken.blog`와 맞추도록 기존 smoke 수정 후 재검증 대상. 원격 CI 전체 성공으로 표시하지 않음.

CI smoke의 브랜드 기대값·추가 정적 경로 검사를 보완한 뒤 동일 workflow shell을 로컬에서 실행해 성공. 기존 API CI job은 36223261409에서 성공. 수정 커밋 author 2026-06-17 21:10 +09:00, 실제 검증·push 2026-09-26 UTC.

### 원격 최종 확인

2026-09-26 UTC main `fa86abc` 정상 반영. CI [36223462342](https://github.com/gjaku1031/ken-blog/actions/runs/36223462342)·Pages [36223462339](https://github.com/gjaku1031/ken-blog/actions/runs/36223462339) 성공. 공개 6경로 HTTP 200, API 주소 미설정 상태 유지. 기존 운영 API 교체 없음.
